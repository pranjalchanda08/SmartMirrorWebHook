# This is a sample build configuration for Python.
# Check our guides at https://confluence.atlassian.com/x/x4UWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
# image: python:3.7.2

# pipelines:
#   default:
#     - step:
#        name: Json
#        script:
#           - echo "{}" > json/fnReg.json
#           - pip install -r requirements.txt
#           - chmod 777 dev/buildall.sh
#           - bash dev/buildall.sh

#     - step:
#         name: Build target
#         script:
#           - tar -czvf build/build_package.tar.gz app.py Procfile requirements.txt json dev
#         artifacts:
#           - build/build_package.tar.gz

#     - step:
#         name: Deploy to production
#         deployment: production
#         caches:
#           - pip
#         script:
#           - pipe: atlassian/heroku-deploy:0.1.1
#             variables:
#               HEROKU_API_KEY: $HEROKU_API_KEY
#               HEROKU_APP_NAME: $HEROKU_APP_NAME
#               ZIP_FILE: build/build_package.tar.gz

